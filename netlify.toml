[build]
  command = "npm run build"
  publish = "dist"

# Force the free *.netlify.app subdomain to redirect to the real domain.
[[redirects]]
  from = "https://splitframejs.netlify.app/*"
  to = "https://splitframe.johansuryanto.dev/:splat"
  status = 301
  force = true
